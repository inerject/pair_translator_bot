{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run proj",
            "type": "debugpy",
            "request": "launch",
            "module": "pair_translator_bot.main",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}